# Hidden Style Library

This folder is server-only and not exposed in the UI.

Populate each leaf folder with curated "golden" reference images using the filenames
defined in `lib/styleLibrary.ts` (or update that index to match your file names).

When no user moodboard is provided, the Reference Matcher node selects 2-3 images
from this hidden library and injects them into generation automatically.
